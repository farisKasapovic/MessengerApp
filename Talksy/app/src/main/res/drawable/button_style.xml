<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
<solid android:color="@android:color/white" /> <!-- Pozadina (bela) -->
<stroke
    android:width="2dp"
    android:color="@android:color/darker_gray" /> <!-- Border širina i boja -->
<corners android:radius="8dp" /> <!-- Zaobljeni uglovi -->
</shape>
